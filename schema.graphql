# user token balance
type User {
    id: ID!
    balance: BigInt!
    balancePrecision: Float!
}

# token total supply snapshot per block
type TotalSupplySnapshot {
    id: ID!
    totalSupply: BigInt!
}

type Storage {
    id: ID!
    lastBlockNumber: Int!
}